spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/maxsulot1
    username: postgres
    password: 1273
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
      format_sql: true

  application:
    name: tracinglogs
server:
  port: 8080